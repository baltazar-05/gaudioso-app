name: gaudioso_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1  # ← REMOVA OS DOIS PONTOS NO FINAL

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  intl: ^0.20.0
  provider: ^6.0.5
  cupertino_icons: ^1.0.8
  flutter_localizations: 
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true